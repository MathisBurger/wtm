{% extends 'navbarBase.html.twig' %}

{% block body %}
    <div class="container mt-5">
        <h1>Berichte</h1>
        <table class="table">
            <thead>
            <tr>
                <th scope="col">Zeitraum</th>
                <th scope="col">Aktionen</th>
            </tr>
            </thead>
            <tbody>
            {% for report in reports %}
                <tr>
                    <td>{{ report }}</td>
                    <td>
                        <div class="btn-group">
                            <a href="{{ path("report_generate", {period: report}) }}" class="btn btn-sm btn-primary">Öffnen</a>
                        </div>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}